# Copy to .env.local to override defaults.
# Backend address used by the Vite dev/preview proxy for /api/** (default http://localhost:8080)
VITE_BACKEND_URL=http://localhost:8080
# Base URL the browser uses for API calls. Leave empty to go through the proxy (recommended in dev).
VITE_API_BASE_URL=
